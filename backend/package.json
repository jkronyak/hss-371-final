{
  "name": "backend",
  "version": "1.0.0",
  "description": "HSS 371 Final Backend",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "client": "cd app && npm install && npm start",
    "server": "npm install && npm start",
    "startall": "concurrently \"npm run client\" \"npm run server\" "
  },
  "author": "Jared Kronyak and Erik Anhorn",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.5.0",
    "express": "^4.18.2"
  }
}
